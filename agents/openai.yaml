interface:
  display_name: "X-Border Listing Image Skill"
  short_description: "多平台 Listing 内容、图片策划与规范预检"
  default_prompt: "Use $xborder-image-skill to prepare a listing package for my target marketplace and country. Ask for the category if it affects required attributes."

policy:
  allow_implicit_invocation: true
